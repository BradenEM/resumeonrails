<div id="<%= dom_id resume %>">

    <div class="mb-6 border-b pb-4">
        <div class="text-2xl font-bold text-gray-800 mb-1"><%= resume.name %>
            <div class="text-gray-600 text-sm"><%=resume.email%> | <%= resume.phone%>
            </div>

        </div>

    </div>