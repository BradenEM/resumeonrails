<h2>Jobs</h2>

<div id="jobs">
    <%= render resume.jobs %>
</div>

<div data-controller="toggle">
    <button data-toggle-target="button" type="button" data-action="click->toggle#toggle" class="">
        Add New Job</button>
    <div data-toggle-target="toggleable" class="hidden">
        <h3>New Job:</h3>
        <%= render "jobs/new", resume: resume %>
    </div>

</div>